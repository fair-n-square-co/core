# Agents

## Source of Truth

The canonical documentation for the entire project (of which `core` is one service) lives in the **docs repo** at `../docs`.

If you make a substantial change — new domain concepts, API contracts, schema changes, architectural decisions — update the docs repo accordingly to keep it in sync.

# Copy to .env and adjust before running `docker compose up`.
# .env is git-ignored; never commit real credentials.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=core

# App connection string. Host must match the compose service name (db).
CORE_DB_CONNSTRING=postgres://postgres:change-me@db:5432/core?sslmode=disable
